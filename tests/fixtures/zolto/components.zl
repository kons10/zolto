---
title: Component Fixture — Phase 9
author: Zolto Test Suite
---

# Component Syntax — Phase 9

component Card(title="", subtitle="", variant="default")
card variant=variant
### {title}
{subtitle}
slot
end
end

Card(title="Welcome to Zolto Components", subtitle="Phase 9 Architecture", variant="primary")
This is the body content injected into the default slot.
end

macro note(text)
info
{text}
end

note("Phase 9 component and macro system active.")
