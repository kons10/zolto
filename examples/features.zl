---
title: Zolto Full Feature Showcase — Phase 6
---

@var engine = Zolto
@var phase  = 6

# {{engine}} Phase {{phase}} Full Showcase

---

## 1. Native Charts Engine (Phase 6)

@chart bar title="Quarterly Sales Performance" theme="custom:neo"
labels:
  Q1
  Q2
  Q3
  Q4

data:
  120
  180
  240
  310
@/chart

---

## 2. Native Diagram Engine (Phase 5)

@diagram flowchart title="System Workflow" theme="neo"
Start[Start Session] -> Auth{Authenticated?}
Auth -- Yes --> Dashboard[Load Dashboard]
Auth -- No --> Login[Prompt Login]
Login -> Auth
@/diagram

---

## 3. Native Math Engine (Phase 4)

Inline math: $E = mc^2$ and Euler's identity $e^{i\pi} + 1 = 0$.

@math label="eq:einstein"
E = \frac{m_0 c^2}{\sqrt{1 - \frac{v^2}{c^2}}}
@/math

Reference to Equation @ref(eq:einstein).

---

## 4. Callouts & Admonitions (Phases 2-3)

> [!TIP]
> Zolto is a high-performance document engine supporting native math, diagrams, charts, and directives.

[info title="Zolto Engine Info"]
100% backward compatible with CommonMark and GitHub Flavored Markdown.
[/info]

---

## 5. Definition List & Code Metadata

```javascript title="app.js" {2} numbers
import { compile } from 'zolto';
const html = compile(zoltoSourceStr);
console.log(html);
```

Callout
: A styled blockquote using `> [!TYPE]` syntax.

Chart
: A native `@chart <type> ... @/chart` data visualization block.
